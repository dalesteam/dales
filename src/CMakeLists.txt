set(target_name dales4)
FILE(GLOB sourcefiles "*.f90" "*.F90")
add_executable(${target_name} ${sourcefiles})
target_link_libraries(${target_name} ${NETCDF_LIBS} ${LIBS} ${EXTLIB})
install(TARGETS dales4 DESTINATION ${CMAKE_BINARY_DIR})
