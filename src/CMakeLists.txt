set(target_name daleslib)
FILE(GLOB sourcefiles "*.f90")
add_executable(${target_name} ${sourcefiles})
target_link_libraries(${target_name} ${NETCDF_LIBS})
install(TARGETS daleslib DESTINATION ${CMAKE_BINARY_DIR})
